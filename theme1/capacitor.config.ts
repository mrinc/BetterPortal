import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cloud.betterportal.mobile',
  appName: '@bettercorp/betterportal',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
